let data; // csv data

let subset_selection = []; // id, subset_selection, included in structural learning
let blacklist = []; //array of objects with attributes: from and to
let whitelist = []; //array of objects with attributes: from and to

let learned_structure_data= null;
let learned_structure_data_without_modifications = null;
let node_distinction = null;
